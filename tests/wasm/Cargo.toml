[package]
name = "jks-wasm-test"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib"]
path = "lib.rs"

[dependencies]
jks = { path = "../..", default-features = false }

[profile.release]
opt-level = "z"
lto = true
